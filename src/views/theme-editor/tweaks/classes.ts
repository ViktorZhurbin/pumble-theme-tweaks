const baseButtonClasses = "btn btn-xs btn-ghost btn-circle";

export const bodyButtonClasses = `${baseButtonClasses} opacity-60 hover:opacity-100`;

export const headerButtonClasses = `${baseButtonClasses}`;
